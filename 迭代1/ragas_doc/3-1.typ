// TODO @wh
// 请参考 `3-1.typ` 中 `核心模块` 的表格格式完成文档

== 核心模块

#table(
  columns: (10%, 10%, 10%, 70%),
  align: (center, center, center, center),
  table.header[*包*][*模块*][*类*][*描述*],
  [`A`], [`BB`], [`CCC`], [这是描述],
)


== 指标系统
