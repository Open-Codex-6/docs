= 项目功能描述

Ragas 作为一个开源的 Python 工具包，旨在将大语言模型（LLM）应用中传统的主观评审转变为系统化、可重复的评估工作流。

其核心功能围绕三个维度展开：首先，Ragas 提供了一套结合 LLM 评测与传统指标的评估体系，专门用于量化 RAG 管道及 Agents 的输出质量；其次，Ragas 能够利用知识图谱与查询合成技术，从非结构化文档中自动生成合成测试数据集；最后，通过内置的适配器，Ragas 实现了与主流 LLM 供应商、嵌入模型及 LangChain、LlamaIndex 等开发框架的无缝集成。

== 管理评估样本数据

=== 创建与管理评估数据

用户可以通过代码、`.json` 文件、`.csv` 文件、`DataFrame` 等多种来源导入评估数据，并组织成结构化的数据集，便于后续分析和评估。数据集支持多种存储后端，包括内存存储、本地文件系统、Google Drive 等。

=== 数据集导出与集成

数据集可以灵活导出为 `.json`、`.csv`、`DataFrame` 等格式，方便与其他数据分析工具和机器学习框架集成。支持批量操作和数据持久化，确保评估数据的可追溯性和可复用性。

== 评估指标体系

=== 丰富的预置指标

Ragas 提供超过 30 种预置评估指标，覆盖 RAG 系统、AI Agent、多模态应用等多种场景。包括：

- *RAG 评估指标*：Faithfulness（忠实度）、Context Precision（上下文精确度）、Context Recall（上下文召回率）、Answer Relevance（答案相关性）
- *Agent 评估指标*：Tool Call Accuracy（工具调用准确度）、Goal Accuracy（目标达成度）
- *通用指标*：AspectCritic（方面评判）、Answer Correctness（答案正确性）、BLEU、ROUGE 等

=== 自定义指标能力

支持用户根据特定业务需求创建自定义评估指标。通过继承基础 `Metric` 类，用户可以定义自己的评分逻辑、提示词模板和评估标准，实现灵活的评估体系扩展。

== 模型评估执行

=== 同步与异步评估

提供 `evaluate()` 和 `aevaluate()` 两种评估接口，支持同步和异步评估执行。可以批量评估大规模数据集，自动管理并发执行和资源分配，提高评估效率。

=== 多维度评估报告

自动生成详细的评估结果（`EvaluationResult`），包括每个样本的评分、统计信息、错误追踪等。支持进度显示、异常处理和结果持久化，帮助用户全面分析模型性能。

=== 灵活的 LLM 配置

支持多种 LLM 提供商（OpenAI、Anthropic、Azure、AWS Bedrock 等）和嵌入模型配置。通过 `llm_factory` 和 `embedding_factory` 实现统一的模型接口，方便切换和比较不同模型的评估效果。

== 实验管理与版本控制

=== 实验追踪

通过 `Experiment` 模块系统化管理多次评估实验，追踪不同版本、不同参数配置下的模型表现。支持实验命名、结果对比和历史记录查询。

=== 版本控制集成

提供 `version_experiment()` 函数，与 Git 版本控制系统深度集成。自动创建实验分支、提交代码快照，确保实验的可复现性。每次实验都关联具体的代码版本，便于追溯和回滚。

=== 实验结果存储

实验结果可持久化到多种后端存储，支持长期保存和跨团队共享。通过统一的 `Backend` 接口，支持本地文件、云存储等多种存储方案。

== 测试数据自动生成

=== 智能数据合成

使用 `TestsetGenerator` 自动生成高质量的测试数据集。基于知识图谱和 LLM 能力，生成覆盖多种场景的测试样本，包括简单问答、推理问题、多跳问题等。

=== RAG 和 Agent 场景支持

针对不同的应用场景提供专门的数据生成策略：

- *RAG 系统*：生成问题-上下文-答案三元组，覆盖不同难度级别
- *Agent 应用*：生成需要工具调用、多步推理的复杂测试场景

=== 可配置的生成策略

支持自定义数据生成参数，如问题类型分布、难度级别、数据量等。通过 `Synthesizers` 和 `Transforms` 模块，用户可以定制数据生成流程。


== 框架集成与可观测性

=== 主流框架集成

与多个主流 LLM 应用开发框架无缝集成：

- *LangChain/LangGraph*：直接评估 Chain 和 Agent
- *LlamaIndex*：评估索引和查询引擎
- *Griptape、Swarm*：支持多 Agent 系统评估
- *R2R*：RAG 系统专项集成

=== 可观测性工具集成

与生产环境监控工具深度集成，构建完整的评估-监控-优化闭环：

- *LangSmith、Opik*：实时追踪评估过程
- *Helicone*：LLM 调用监控和成本分析
- *AG UI*：Agent 应用可视化调试

=== 统一的回调接口

提供标准化的回调机制，用户可以接入自定义的日志、监控和告警系统，实现评估过程的全面可观测。

