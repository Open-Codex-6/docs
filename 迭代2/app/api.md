# APP API 接口文档

## 需求/用例

- 注册：输入用户名和密码进行注册，注册成功后返回用户 id，失败时提示创建失败。
- 登录：输入用户名和密码登录，成功后返回 token 供后续使用，失败时提示用户不存在或密码错误。
- 更新用户信息：登录后携带 token 提交新的用户名和密码进行更新，成功时提示更新完成；若 token 无效、过期或与用户名不匹配则提示重新登录或校验失败。
